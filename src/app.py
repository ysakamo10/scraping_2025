# src/app.py の冒頭
import streamlit as st
# from scraping.url_helpers import fetch_url

st.title("URL 取得テスト")
# url = st.text_input("URL を入力")
# if st.button("取得"):
#     html = fetch_url(url)
#     st.code(html[:200] + "…", language="html")
